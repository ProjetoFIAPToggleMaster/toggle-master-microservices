# Template. Veja explicacao no auth/secret.example.yaml e no RUNBOOK.
apiVersion: v1
kind: Secret
metadata:
  name: flag-secret
  namespace: flags
type: Opaque
stringData:
  # endpoint do RDS toggle-flag (porta 5432, banco flags_db)
  DATABASE_URL: "postgres://postgres:REPLACE_ME_FLAG_DB_PASS@REPLACE_ME_FLAG_ENDPOINT:5432/flags_db"
