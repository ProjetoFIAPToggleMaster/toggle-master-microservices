# Template. Veja explicacao no auth/secret.example.yaml e no RUNBOOK.
apiVersion: v1
kind: Secret
metadata:
  name: targeting-secret
  namespace: targeting
type: Opaque
stringData:
  # endpoint do RDS toggle-targeting (porta 5432, banco targeting_db)
  DATABASE_URL: "postgres://postgres:REPLACE_ME_TARG_DB_PASS@REPLACE_ME_TARG_ENDPOINT:5432/targeting_db"
