apiVersion: v1
kind: ConfigMap
metadata:
  name: analytics-config
  namespace: analytics
data:
  PORT: "8005"
  AWS_REGION: "us-east-1"
  # nome fixo esperado pelo codigo (analytics-service/README.md)
  AWS_DYNAMODB_TABLE: "ToggleMasterAnalytics"
