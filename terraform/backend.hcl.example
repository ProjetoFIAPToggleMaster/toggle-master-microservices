# Copie para backend.hcl e preencha com os seus valores.
# Uso: terraform init -backend-config=backend.hcl

bucket  = "toggle-master-tfstate-fiap-2026"
key     = "infra/terraform.tfstate"
region  = "us-east-1"
encrypt = true
profile = "toggle-master"
